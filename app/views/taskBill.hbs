<!DOCTYPE html>
<html>

<head>
    <script type="text/javascript" src="/js/bill.js"></script>
    <meta http-equiv="content-type" content="text/plain; charset=UTF-8" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB"
        crossorigin="anonymous">
    <title>{{title}}</title>
</head>

<body class="text-center">
    <main role="main">
        {{>menu}}
        <div class="container">
            {{!--
            <section class="jumbotron text-center"> --}}
                <h2 class="display-4 font-italic">{{title}}</h2>
                <p>за период {{dateFrom}} - {{dateTill}}</p>
                <p>Дата счета: {{dateNow}}</p>
                <table class="table table-hover" name="rows" id="rows">
                    <tr>
                        <td>Работодатель</td>
                        <td>Задача</td>
                        <td>Работник</td>
                        <td>Валюта</td>
                        <td>Оплата</td>
                    </tr>

                    {{#each rows}}
                    <tr>
                        <td>{{this.employer}}</td>
                        <td>{{this.task}}</td>
                        <td>{{this.worker}}</td>
                        <td>{{this.currency}}</td>
                        <td>{{this.payment}}</td>
                    </tr>{{/each}} {{#each hours}}
                    <tr>
                        <td>{{hour}}</td>
                        {{#each minutes}}
                        <td>{{setWorkType isGoodWork}}</td>
                        {{/each}}
                    </tr>
                    {{/each}}
                </table>
                <br>
                <input class="btn btn-primary btn-lg btn-block" type="button" id="idBill" name="btnBill" value="Печать" 
                onClick="print()"> {{!-- </section> --}}
        </div>
    </main>
    <br> {{>footer}}
</body>

</html>