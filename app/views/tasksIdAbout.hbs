<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB"
        crossorigin="anonymous">
    <meta http-equiv="content-type" content="text/plain; charset=UTF-8" />
    <title>Рабочий журнал</title>
</head>

<body class="text-center">
    <main role="main">
        {{>menu}}
        <div class="container">
            <section class="jumbotron text-center">
                <h2 class="display-4 font-italic">Рабочий журнал</h2>
                <h2 class="display-6 font-italic">Проект "{{task_title}}"</h2>
                <br> {{#if rWorkerIsNotEmpty}}
                <div>
                    <form action="/tasks/{{id_task}}/journal" method="post">
                        <label for="worker">Выберите работника</label>
                        <select class="custom-select d-block w-100" name="worker" id="worker">
                            {{createCbWorker rWorker}}
                        </select>
                        <br>
                        <br>
                        <label for="date">Выберите дату</label>
                        <input class="form-control" type="date" name="date" value={{DateNow}}/>
                        <br>
                        <input class="btn btn-primary btn-lg btn-block" type="submit" value="Просмотр">
                        <br>
                    </form>
                </div>
                {{else}}
                <p>Работников нет</p>
                {{/if}}
            </section>
        </div>
    </main>
    {{>footer}}
</body>

</html>