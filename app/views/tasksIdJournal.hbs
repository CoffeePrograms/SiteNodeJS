<!DOCTYPE html>
<html>

<head>
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB"
        crossorigin="anonymous">
</head>

<body class="text-center">
    <main role="main">
        {{>menu}}
        <div class="container">
            {{!-- <section class="jumbotron text-center"> --}}

                <h2 class="display-4 font-italic">{{title}}</h2>
                <p class="display-6 font-italic">Работник: {{worker_title}}</p>
                <p class="display-6 font-italic">Дата: {{dateW}}</p>
                <table class="table table-hover table-bordered table-striped">
                    <tr>
                        <td rowspan="2">Часы</td>
                        <td colspan="13">Минуты</td>
                    </tr>
                    <tr>
                        {{#each minutesF}}
                        <td>{{this}}</td>
                        {{/each}}
                    </tr>
                    {{#each hours}}
                    <tr>
                        <td>{{hour}}</td>
                        {{#each minutes}}
                        <td>{{setWorkType isGoodWork}}</td>
                        {{/each}}
                    </tr>
                    {{/each}}
                </table>
            {{!-- </section> --}}
        </div>
    </main>
    {{>footer}}
</body>

</html>